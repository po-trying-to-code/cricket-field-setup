name: Deploy team-planner to Vercel

on:
  push:
    branches:
      - team-planner

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger Vercel deploy hook
        run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK_TEAM_PLANNER }}"
